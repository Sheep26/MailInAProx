volumes:
  db:

services:
  mailonapi:
    image: mailonapi:latest
    container_name: mailonapi
    restart: always
    ports:
      - "6780:8080"

  mailonapi-db:
    image: mysql:latest
    container_name: mailonapi-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: putyourshere
    ports:
      - "3308:3306"
    volumes:
      - type: volume
        source: db
        target: /var/lib/mysql