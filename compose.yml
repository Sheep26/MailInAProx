volumes:
  db:

services:
  mailonapi:
    image: mailonapi:latest
    container_name: mailonapi
    restart: always
    ports:
      - "6780:8080"

  mailonapidb:
    image: mysql:latest
    container_name: wchouses-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: Pu#rari4
    ports:
      - "3307:3306"
    volumes:
      - type: volume
        source: db
        target: /var/lib/mysql